// @routecraft/os -- system-native host capabilities.
// Planned members: shell (sandboxed by default), sandbox, browser automation
// (agentBrowser, merging in from @routecraft/browser), and future host
// capabilities such as clipboard and notifications. Adapters land in future PRs.
export {};
