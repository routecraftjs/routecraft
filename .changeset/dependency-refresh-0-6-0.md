---
"@routecraft/ai": patch
"@routecraft/cli": patch
"create-routecraft": patch
---

Refresh dependency floors to the latest minor and patch releases ahead of 0.6.0. Runtime ranges that moved: `ai` (`@routecraft/ai`), `@opentelemetry/sdk-trace-base`, `fast-xml-parser`, `imapflow`, `jose` and `mailparser` (`@routecraft/cli`), and `@inquirer/prompts` (`create-routecraft`). Optional peer ranges keep their existing wide floors, and no major upgrades are included.
