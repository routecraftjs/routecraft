version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10

  # While Deno itself doesn't use traditional package management,
  # we'll monitor GitHub Actions dependencies
    commit-message:
      prefix: "chore"
      include: "scope" 