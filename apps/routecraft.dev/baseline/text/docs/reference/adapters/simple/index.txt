simple · Docs - Routecraft Routecraft Docs Blog Cheat sheet Changelog Search Getting Started Installation Changelog Introduction What is Routecraft Project structure Capabilities The Exchange Operations Adapters Events Testing Deployment Monitoring Terminal UI Advanced Plugins Composing Capabilities Error Handling Filter Chain Merged Options Type Registries Creating adapters Programmatic Invocation Running an MCP server Calling an MCP Judging Agent Results Securing capabilities Linting Formatting Reference Adapters Operations Events Configuration CLI Runtime Plugins Linting Errors Examples File to HTTP MCP tool Support triage agent Community Contribution Guide FAQ v0.6.0 Ask Claude simple ← All adapters simple < T > ( producer : ( ( ) => T | Promise < T > ) | T ) : Source < T > Copy Create a static or dynamic data source. When the producer returns an array , each element becomes a separate exchange processed independently through the pipeline. // Static value . id ( 'hello-route' ) . from ( simple ( 'Hello, World!' ) ) // Array of values (each becomes a separate exchange) . id ( 'items-route' ) . from ( simple ( [ 'item1' , 'item2' , 'item3' ] ) ) // Dynamic function . id ( 'api-route' ) . from ( simple ( async ( ) => { const response = await fetch ( 'https://api.example.com/data' ) return response . json ( ) } ) ) // With custom ID . id ( 'data-loader' ) . from ( simple ( ( ) => loadData ( ) ) ) Copy Use cases: Testing, static data, API polling, file reading
